import styled from 'styled-components'

export const PageHeaderContainer = styled('div')`
  margin-bottom: 1rem;

  > .backButton {
    margin-left: 1.2rem;
  }
  > .title {
    margin-left: 2rem;
    font-weight: 900;
  }
`